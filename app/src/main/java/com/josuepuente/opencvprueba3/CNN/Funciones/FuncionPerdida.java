package com.josuepuente.opencvprueba3.CNN.Funciones;
/*
import org.nd4j.linalg.api.ndarray.INDArray;
import org.tensorflow.lite.Tensor;

public abstract class FuncionPerdida {


     * Calcula la función de pérdida entre las predicciones y las etiquetas verdaderas.
     *
     * @param predicciones las predicciones de la red neuronal (un tensor 4D con forma [batch_size, num_clases, altura_imagen, ancho_imagen])
     * @param etiquetas las etiquetas verdaderas (un tensor 1D con forma [batch_size])
     * @return la función de pérdida entre las predicciones y las etiquetas verdaderas

    public abstract float calcular(Tensor predicciones, Tensor etiquetas);


     * Calcula el gradiente de la función de pérdida con respecto a las predicciones.
     *
     * @param predicciones las predicciones de la red neuronal (un tensor 4D con forma [batch_size, num_clases, altura_imagen, ancho_imagen])
     * @param etiquetas las etiquetas verdaderas (un tensor 1D con forma [batch_size])
     * @return el gradiente de la función de pérdida con respecto a las predicciones (un tensor 4D con la misma forma que las predicciones)

    public abstract Tensor calcularGradiente(Tensor predicciones, Tensor etiquetas);

    public double calcularPerdida(INDArray[] salida, INDArray zeros) {

        return 0;
    }

}
*/